name: iouring_transport
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dev_dependencies:
  ffigen: ^7.0.0
  test: ^1.22.0

dependencies:
  ffi: ^2.0.1
  path: ^1.8.3

ffigen:
  name: "TransportBindings"
  description: "Bindings for Transport"
  output: "lib/transport/bindings.dart"
  headers:
    entry-points:
      - "../binding/binding_transport.h"
      - "../binding/binding_listener.h"
      - "../binding/binding_channel.h"
      - "../binding/binding_file.h"
      - "../binding/binding_socket.h"
  globals:
    exclude:
      - "_(.*)"
      - "ck(.*)"
  functions:
    symbol-address:
      include:
        - ".*"
    exclude:
      - "_(.*)"
      - "ck(.*)"
      - "io_uring(.*)"
  macros:
    exclude:
      - "_(.*)"
      - "true"
      - "false"
      - "CK(.*)"
  typedefs:
    exclude:
      - "_(.*)"
      - "ck(.*)"
  structs:
    exclude:
      - "_(.*)"
      - "max_align_t"
      - "ck(.*)"
      - "file_(.*)"
      - "io_uring_probe"
