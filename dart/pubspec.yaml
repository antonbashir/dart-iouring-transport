name: iouring_transport
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dev_dependencies:
  ffigen: ^7.2.5
  test: ^1.22.0

dependencies:
  ffi: ^2.0.1
  path: ^1.8.3

ffigen:
  name: "TransportBindings"
  description: "Bindings for Transport"
  output: "lib/transport/bindings.dart"
  headers:
    entry-points:
      - "../native/transport_constants.h"
      - "../native/transport_transport.h"
      - "../native/transport_channel.h"
      - "../native/transport_acceptor.h"
      - "../native/transport_connector.h"
      - "../native/transport_file.h"
      - "../native/transport_socket.h"
  globals:
    exclude:
      - "_(.*)"
      - "ck(.*)"
  functions:
    symbol-address:
      include:
        - ".*"
    exclude:
      - "_(.*)"
      - "ck(.*)"
      - "io_uring(.*)"
  macros:
    exclude:
      - "_(.*)"
      - "true"
      - "false"
      - "CK(.*)"
  typedefs:
    exclude:
      - "_(.*)"
  structs:
    exclude:
      - "_(.*)"
      - "max_align_t"
      - "file_(.*)"
      - "io_uring_probe"
